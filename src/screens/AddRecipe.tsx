import { Layout, Text } from "@ui-kitten/components";
import React from "react";

export default class AddRecipe extends React.Component<any, any> {
    render() {
        return (
            <Layout>
                <Text>Ajouter une recette</Text>
            </Layout>
        )
    }
}